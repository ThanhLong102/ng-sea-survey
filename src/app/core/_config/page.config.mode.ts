export class PageConfig {
  api: {
    baseUrl: string,
  };
  pageSizeOptions: {
    size: any
  };
  pageIndex: { basePage: number };
  pageSize: { defaultPageSize: number };
  token: { token: string };
}
